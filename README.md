# cloud-symlinks

Application to tar linux symlinks that are not correctly sync by cloud storage providers into a file that is syncable 
and then un tar if this file is changed

I use it to keep my own books and articles library where some of them are also managed with Zotero 
